name: React Native Windows
on:
  push:
    branches: [master]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  build:
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: clarora-app/package-lock.json
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x
      - uses: microsoft/setup-msbuild@v2
      - uses: nuget/setup-nuget@v2
      - run: npm ci
        working-directory: clarora-app
      - name: Build on-device ASR library (clarora_asr.dll)
        continue-on-error: true  # 首次引入的原生构建；失败不阻塞应用包，端侧转写运行时降级
        run: powershell -NoProfile -ExecutionPolicy Bypass -File scripts/build-windows-asr.ps1
        working-directory: clarora-app
      - run: npm run windows:build
        working-directory: clarora-app
      - uses: actions/upload-artifact@v4
        with:
          name: Clarora-Windows-x64-unsigned
          path: clarora-app/windows/AppPackages/**/*
          if-no-files-found: error
